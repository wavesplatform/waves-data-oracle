export const enum STATUSES {
    OK = 'ok',
    EMPTY = 'empty',
    ERROR = 'error'
}

export const enum DATA_TRANSACTION_FIELD_TYPE {
    INTEGER = 'integer',
    BOOLEAN = 'boolean',
    STRING = 'string',
    BINARY = 'binary'
}

export const DEFAULT_LANG = 'en';
